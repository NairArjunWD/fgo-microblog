<%= form_with(model: rayshift, local: true) do |form| %>
  <% if rayshift.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(rayshift.errors.count, "error") %> prohibited this rayshift from being saved:</h2>

      <ul>
        <% rayshift.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :post %>
    <%= form.text_area :post %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
